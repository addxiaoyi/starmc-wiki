# StarMC Wiki 贡献指南

文档放在 `/public/content/wiki/` 目录下。

## 要点

- 新建 `.md` 文件，文件名对应页面路径。
- 保留必要元数据。
- 使用标准 Markdown 编写内容。
- 提交后由部署流程更新站点。

## 元数据

```markdown
TITLE: 页面标题
CATEGORY: 分类名称
LAST_UPDATED: YYYY-MM-DD
PARENT: 父级页面（可留空）
ICON: 页面图标（可选）
```

## 参考

可直接查看 `/wiki/template`。